Algoritmo "semnome"

Var

  n, i, x, soma : inteiro

Inicio

  escreval("Digite o primeiro numero : ")
  leia(n)
  
  soma <- 0
  para i de 1 ate n faca
    escreva("Digite um número : ")
    leia(x)
    soma <- soma + x
  fimpara
  
  escreval("Soma =", soma)

Fimalgoritmo